name: neural_annealing
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - --upgrade pip
    - --upgrade "jax[cuda12]"
    - torch
    - torchvision
    - tensorboard
    - black